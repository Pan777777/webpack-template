include ../utils/mixins
block variables 

doctype html
html(lang="en")
    head
        meta(charset='utf-8')
        meta(name='viewport' width='device-width')
        title #{title}
        meta(name='discription', content=discription)
    body
        .wrapper
            include ../ul-kit/headers&footers.pug